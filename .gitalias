[alias]
        a = add

        amend = commit --amend

        b = branch

        c = commit

        co = checkout
        cob = checkout -b

        d = diff --color

        ed = branch --no-merged dev

        g = !git gui

        k = !gitk --all

        lc = !git ol ORIG_HEAD.. --stat --no-merges

        m = merge --no-ff -e
        mff = merge

        ol = log --pretty=oneline --abbrev-commit --graph --decorate
        olnp = !git --no-pager ol

        p = push

        pl = pull

        rb = rebase

        s = status

        st  = stash
        stl = stash list
        sta = stash apply
        std = stash drop
        sts = stash show -p
        stp = stash pop

        unc = reset --soft HEAD^

        who = shortlog -sne
